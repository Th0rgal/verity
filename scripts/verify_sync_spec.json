{
  "check_only_paths": [
    ".github/ISSUE_TEMPLATE/**",
    "docs/**",
    "docs-site/**",
    "AUDIT.md",
    "AXIOMS.md",
    "README.md",
    "TRUST_ASSUMPTIONS.md"
  ],
  "compiler_paths": [
    ".github/actions/**",
    ".github/workflows/verify.yml",
    "Verity/**",
    "Compiler/**",
    "Compiler.lean",
    "Contracts/**",
    "Contracts.lean",
    "examples/**",
    "packages/**",
    "test/**",
    "scripts/**",
    "lakefile.lean",
    "lake-manifest.json",
    "lean-toolchain",
    "foundry.toml"
  ],
  "expected_jobs": [
    "changes",
    "checks",
    "build",
    "build-compiler",
    "lean-profile",
    "foundry-gas-calibration",
    "foundry",
    "foundry-patched",
    "foundry-multi-seed",
    "failure-hints"
  ],
  "expected_checks_commands": [
    "make check"
  ],
  "expected_checks_other_commands": [],
  "expected_build_commands": [
    "check_split_package_builds.py",
    "check_lean_warning_regression.py --log lake-build.log",
    "check_proof_length.py --format=markdown >> $GITHUB_STEP_SUMMARY",
    "report_property_coverage.py --format=markdown >> $GITHUB_STEP_SUMMARY",
    "check_storage_layout.py --format=markdown >> $GITHUB_STEP_SUMMARY"
  ],
  "expected_build_compiler_commands": [
    "generate_yul_identity_diff_report.py --verity-dir compiler/yul-parity-pack --solc-dir compiler/yul-parity-reference --output compiler/parity-pack-identity-report.json",
    "check_parity_pack_metrics.py --report compiler/parity-pack-identity-report.json --max-only-in-verity 0 --max-only-in-solidity 0 --max-hash-mismatch 0 --format markdown >> \"$GITHUB_STEP_SUMMARY\"",
    "check_gas.py coverage --dir compiler/yul --dir compiler/yul-patched",
    "keccak256.py --self-test",
    "check_selectors.py --check-fixtures",
    "check_yul.py --dir compiler/yul --dir compiler/yul-patched --require-same-files compiler/yul compiler/yul-patched",
    "check_gas.py report",
    "check_patch_gas_delta.py --baseline-report gas-report-static.tsv --patched-report gas-report-static-patched.tsv --min-improved-contracts 0 --summary-markdown patch-gas-delta-summary.md >> \"$GITHUB_STEP_SUMMARY\""
  ],
  "expected_foundry": {
    "shards": 8,
    "seed": 42
  },
  "expected_foundry_patched": {
    "seed": 42,
    "shard_count": 1,
    "shard_index": 0,
    "no_match_test": "Random10000"
  },
  "expected_foundry_multi_seed": {
    "seeds": [
      0,
      1,
      42,
      123,
      999,
      12345,
      67890
    ]
  },
  "expected_foundry_gas_calibration": {
    "profile": "difftest",
    "artifact": "static-gas-report",
    "command": "python3 scripts/check_gas.py calibration --static-report gas-report-static.tsv"
  },
  "artifact_producer_jobs": [
    "build",
    "build-compiler"
  ]
}
