---
title: Compiler & CLI
description: Command-line usage, audit-artifact flags, and validated contract set for the Verity compiler.
---

# Compiler & CLI

Reference for the `verity-compiler` binary, the audit-artifact flags, and the validated contract set. For the architecture and pipeline narrative, see [Compiler Architecture](/compiler-architecture). For the trust boundary, see [Trust Model](/trust-model).

## Compile all contracts

```bash
cd /path/to/verity
export PATH="$HOME/.elan/bin:$PATH"

# Build compiler
lake build verity-compiler

# Generate Yul for all contracts
lake exe verity-compiler

# Output: artifacts/yul/*.yul
```

For contracts that use external libraries (e.g. Poseidon hash):

```bash
lake exe verity-compiler --link examples/external-libs/PoseidonT3.yul -o artifacts/yul/
```

See the [Linking External Libraries](/guides/linking-libraries) guide.

## Audit artifacts

The CLI emits machine-readable audit artifacts alongside Yul/ABI output:

- `--trust-report <path>` — proof-boundary and assumption data.
- `--assumption-report <path>` — flattened assumption inventory grouped by contract and usage site. If a constructor or function uses direct assembly-shaped primitives without any `local_obligations [...]`, compilation now fails closed.
- `--layout-report <path>` — resolved storage-slot metadata, including effective alias writes and reserved-slot policies for upgrade/layout reviews.
- `--layout-compat-report <path>` — compares the first selected contract as the baseline layout and the second as the candidate upgrade layout, then writes a machine-readable compatibility summary.

See the [Glossary](/glossary) for one-line definitions of each artifact, and [Trust Model](/trust-model) for what they bound.

## Test compiled contracts

```bash
# Run all Foundry tests (difftest profile enables FFI for Yul compilation)
FOUNDRY_PROFILE=difftest forge test

# Expected: all current difftest suites pass
```

**Lean Proofs**: `lake build` verifies the current proof set. See [VERIFICATION_STATUS.md](https://github.com/Th0rgal/verity/blob/main/docs/VERIFICATION_STATUS.md) for the live totals.

```bash
$ lake build
Build completed successfully.
```

**Coverage**: unit, property, and differential tests across EDSL and compiled Yul. See `test/` for suites.

## Add a new contract

1. **Add a macro contract declaration** in `Contracts/<Name>/<Name>.lean`:

```verity
verity_contract MyContract where
  storage
    value : Uint256 := slot 0
  function setValue (x : Uint256) : Unit := do
    setStorage value x
  function getValue () : Uint256 := do
    return (← getStorage value)
```

If a contract is stateless, the `storage` block may be left empty instead of inventing a dummy slot.

2. **Register it in** `Compiler/Specs.lean` **under** `allSpecs`:

```lean
Contracts.MyContract.spec
```

3. **Recompile**:

```bash
lake build verity-compiler
lake exe verity-compiler
```

4. **Done!** Contract generated in `artifacts/yul/MyContract.yul`.

Legacy note: handwritten `Compiler.Specs.*Spec` definitions are retained only for migration/special workflows and are not the canonical CLI compile path.

For the full authoring checklist (specs, invariants, proofs, tests), see [Add a Contract](/guides/add-contract).

## Validated contracts

All current example contracts compile and pass tests:

- **SimpleStorage** — basic storage operations
- **Counter** — arithmetic operations
- **Owned** — access control with constructor args
- **OwnedCounter** — pattern composition with constructor
- **Ledger** — mapping-based balances
- **SimpleToken** — ERC20-like token with constructor
- **SafeCounter** — checked arithmetic with underflow protection

**Not compiled**: `ReentrancyExample` (proofs are embedded inline, no compiler spec). `CryptoHash` compiles via `--link` with external Poseidon libraries and is validated in CI.

## See also

- [Compiler Architecture](/compiler-architecture) — pipeline narrative, design decisions, SimpleStorage walkthrough.
- [Trust Model](/trust-model) — what the audit artifacts bound.
- [Verification Status](/verification) — live theorem inventory.
- [Examples Gallery](/examples) — the validated contract set with code excerpts.
