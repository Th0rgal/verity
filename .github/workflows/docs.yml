name: Docs

on:
  push:
    branches: [main]
    paths:
      - 'docs-site/**'
  pull_request:
    paths:
      - 'docs-site/**'
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: docs-site
    steps:
      - uses: actions/checkout@v4
      - name: Check docs for Unicode controls
        run: python3 ../scripts/check_unicode_controls.py content

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: docs-site/package-lock.json

      - run: npm ci
      - run: npm run build
